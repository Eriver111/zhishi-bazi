# P2.2 ⑤过耗降权实装验证与 CLOSED 报告

> 状态：**P2.2 CLOSED**（实装与反事实完全一致、全量回归零新增失败）· **未 push 未部署**，待用户明确批准
> 日期：2026-08-13
> 依据：GPT P2.2 最终裁决（九条）；反事实报告 `docs/verification/2026-08-13-p22-counterfactual.md`
> 实装提交：分支 `p2.2-guohao-half`（基于 main 47f6ae6，领先 2 个提交，未 push）
> 验证脚本：`_p2_2b_implementation_verify.js`（B1-B5 全过）

## 一、裁决摘要（GPT P2.2 最终裁决）

1. 正式采纳⑤过耗降权：囚 `(mwxCount-1)×8 → ×4`、休 `(mwxCount-1)×4 → ×2`；不再测试更温和档位。
2. #4 接受为极端地板钳位例外（raw −37→−17 均被 1 分地板吸收，从财格短路⑤不参与输出；不为该盘继续降低全局⑤权重）。
3. P15-19 保留为边界观察盘，不计入 28 盘正式通过标准，不阻塞。
4. H18 27→31 及正财格·破格→成格认定为跨 30 线机械后果，接受。
5. 实装只改⑤两处系数；不碰①休囚/非日支根气/⑧½/P1/structuralRisks。
6. 实装后重跑 28 盘设计样本、18 盘专项观察集、六盘锚点、core test、全量相关回归；若与反事实完全一致且无新增失败，P2.2 CLOSED。
7. 单变量提交，不混入 P2.3；不 push/部署直到用户明确批准。

## 二、实装 diff（单变量）

`js/bazi.js:2311-2312` ⑤块仅两处系数，其余逐字节一致（验证脚本 B1 硬断言：工作区 === HEAD 仅替换⑤块）：

```diff
-    if (WOKE[dgWx] === mwx) score -= (mwxCount - 1) * 8;   // 囚令：日主克月令反被耗（如金克木，木多金缺）
-    else if (WOSHENG[dgWx] === mwx) score -= (mwxCount - 1) * 4; // 休令：日主生月令泄气过重
+    if (WOKE[dgWx] === mwx) score -= (mwxCount - 1) * 4;   // 囚令：日主克月令反被耗（如金克木，木多金缺）
+    else if (WOSHENG[dgWx] === mwx) score -= (mwxCount - 1) * 2; // 休令：日主生月令泄气过重
```

## 三、实装验证（`_p2_2b_implementation_verify.js`，5/5 全过）

| 组 | 内容 | 结果 |
|---|---|---|
| B1 | 单变量提交：工作区 === HEAD 仅⑤块两处系数 8→4/4→2，其余逐字节一致（禁改清单零触碰） | 过 |
| B2 | 正式引擎 47 行 × 6 字段（分/档/用神/喜神/忌神/格局）=== 反事实 CSV P22 侧全等 | 过 |
| B3 | BASE→正式差异集与反事实一致：28 盘翻档恰{P15-12 极弱→偏弱}；28 盘用/喜/忌/格局 0 翻转；18 专项翻档/格局变化恰 H18；用喜忌翻转仅 P15-19 | 过 |
| B4 | GPT 裁决第 8 条预期值全中：P15-12=31偏弱、P15-15=29.75极弱、P15-16=28极弱、B2=27极弱、#8/B1=28极弱、H18=31偏弱+正财格·成格、#4=1极弱 | 过 |
| B5 | ⑥重复表达审计：除#4（比值2.0，钳位掩盖，裁决已接受）外全部 \|⑤\|/\|①\| ≤0.8 | 过 |

⑤命中清单（10 行，与反事实完全一致）：#4（囚×6 −40→−20，可见恒 1）、#8/B1（休×3 24→28）、B2（囚×3 19→27）、P15-12（囚×2 27→31 ⚡翻档）、P15-15/H01（休×2 27.75→29.75）、P15-16（休×2 26→28）、H18（囚×2 27→31 ⚡翻档+正财格 破格→成格）、P15-19（休×3 45→49，用神土→木）。

## 四、回归测试与归因（裁决第 7 条）

| 测试 | 结果 | 归因 |
|---|---|---|
| `node --test tests/bazi-professional-core.test.js` | 26 测：24 过 / 2 败 | 2 败均为预存（wet-earth 26≠32、Zi-hour 0≠1），与 P2.1 实装时同值，零新增 |
| **全量 `node --test`** | 372 测：357 过 / 15 败 | **失败集与 HEAD 基线（同环境 A/B：临时 worktree + node_modules junction）完全一致 → P2.2 零新增失败** |
| `_freeze_p15_6.js` | 3 盘偏离锚点 | 全部为预期 P22 值：P15-12 31≠27（P2.2 新增，预期）、P15-15 29.75≠19（P2.1 预存偏离 19→27.75，P2.2 续至 29.75，预期）、P15-16 28≠26（P2.2 新增，预期）。其余 3 锚点过 |
| `_freeze_22.js` | **未跑** | 该脚本会刷新 `_baseline_22.csv`（纪律禁止）；22 基线盘覆盖已由 B2（47 行全等）承担 |
| `_p2_1f_qualification_freeze.js` | 失败（H01 dayBranchAdj −11.25≠−19） | HEAD worktree 同值验证：**预存于 P2.1 实装**（M-A 改变了 dayBranchAdj，1f 的硬期望为 P1 引擎口径；⑤不涉及 dayBranchAdj，与本轮无关） |
| `_p2_1i_implementation_verify.js` | 失败（预览补丁未生效 hit1=false） | HEAD worktree 同值验证：**预存**——1i 的补丁锚点指向 P2.1 合并前的 HEAD，P2.1 合并后结构上已过期（一次性实装验证脚本） |

## 五、遗留项（不阻塞 CLOSED，均另开任务）

1. **#4 极端钳位例外**（裁决第 3 条已接受）：P22 后⑤仍 −20、比值 2.0，但被 1 分地板完全吸收、从财格短路，可见面零影响。若日后重审⑥重复表达可单列专项。
2. **P15-19 边界观察盘**（裁决第 4 条）：45→49 用神 土→木 翻转，留待后续盲测「边界盘稳定性」观察，不计入 28 盘统计。
3. **`_baseline_22.csv` 用神/喜/忌列刷新**：生成于 P1 冻结（13e588b）之前，P2.1 时已发现 15/22 盘过期，本轮按纪律未刷新。
4. **`_p2_1f`/`_p2_1i` 脚本已随 HEAD 前进而过期**：与 P2.1 记录中 `_freeze_22` 同类（旧版脚本勿修，报告即可）。

## 六、CLOSED 裁定

裁决第 9 条条件全部满足：**正式实装结果与反事实完全一致（47 行 × 6 字段全等，B2）、全量回归零新增失败（372 测失败集与 HEAD 同环境基线一致）、预期变化清单（裁决第 8 条）逐项命中（B4）。**

- 改动窄：两行系数，单变量提交（B1 字节级证明）。
- ⑥重复表达明显收敛：除 #4（钳位例外）外 `|⑤|/|①|` 全部 ≤0.8、单次⑤扣分 ≤8，⑤回归「次级增量修正」定位。
- 28 盘用神/喜神/忌神/格局零翻转，唯一翻档 P15-12（方向正确、GPT 预判）。

**P2.2 CLOSED。**

## 七、部署状态

- 分支 `p2.2-guohao-half` 领先 main 2 个提交（实装 + 本报告），**未 push、未部署**（裁决第 9 条：不 push/部署直到用户明确批准）。
- 批准后部署步骤（P2.1 同流程）：切 main → merge `p2.2-guohao-half`（真实 merge）→ push → webhook 自动部署 → 线上最小验收：
  - P15-12（戊子 甲寅 庚申 丁亥）= 31 偏弱（原 27 极弱，P22 预期变化）
  - P15-15（癸未 戊午 乙卯 丙戌）= 29.75 极弱（原 27.75）
  - P15-03（乙丑 戊寅 己巳 庚午）= 51 中和（锚点零变化盘）
- 下一步：P2.3（待定，不混入本分支）。
